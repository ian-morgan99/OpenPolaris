local.properties
.gradle/
build/
.kotlin/

# Android release signing — never commit passwords.
# Commit key.properties.example as a template; copy to key.properties locally.
androidApp/key.properties

# Copilot / session scaffolding — local-only, not source.
# Track the durable knowledge artifacts (learnings/, skills/) so the
# project memory persists across clones; ignore ephemeral scaffolding.
.agents/**
!.agents/learnings/
!.agents/learnings/**
!.agents/skills/
!.agents/skills/**
.copilot/

# Regression-test fixtures injected by .github/workflows/ci-evidence-lint-regression.yml.
# The workflow creates and deletes this directory in CI; this entry keeps a developer's
# local run from accidentally staging it.
docs/evidence/_regression-fixture/
